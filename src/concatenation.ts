function concatenation(firstWord: string, secondWord: string) {
  console.log(`${firstWord}, ${secondWord}`);
  debugger;
  //? зупиняє виконання коду при виклику функції, після лога у консоль,
  //? у dev tools можна подивитися, які аргументи приймає ф-ція
}

export { concatenation };
